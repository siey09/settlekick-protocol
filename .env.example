# Public devnet configuration
TXLINE_API_ORIGIN=https://txline-dev.txodds.com

# Secret credentials — keep the real values only in .env.local.
# Never commit or paste these into chat, issues, or logs.
TXLINE_API_TOKEN=
TXLINE_GUEST_JWT=
